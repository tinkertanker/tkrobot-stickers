# Tools

Small helper scripts and dependencies live here.

- `scripts/make_contact_sheet.py` builds a labelled contact sheet from a sticker folder.
- `requirements.txt` lists Python dependencies for helper scripts.


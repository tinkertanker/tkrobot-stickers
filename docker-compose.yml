services:
  stickers:
    build:
      context: .
    expose:
      - "8080"
    environment:
      VIRTUAL_HOST: stickers.tk.sg
      VIRTUAL_PORT: 8080
    networks:
      - devtksg
    restart: always

networks:
  devtksg:
    external: true

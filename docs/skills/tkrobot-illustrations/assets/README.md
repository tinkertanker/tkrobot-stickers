# Lock sheets and examples

## Character lock (attach every time)

- `character-lock-canonical.png` — four sticker masters on white (wave,
  hands-on-hips, OK, face). Anatomy and colours.
- `character-lock-sketch.png` — the same mascot in thin-line sketch form.

## Hand lock (attach every time)

Hands are the most common miss. Attach these as well, and prefer them if the
image tool only accepts a few references.

- `hand-lock-canonical.png` — open, point, thumbs-up (palm-closed), closed
  at rest (palm-open, point-right, thumbsup, hands-on-hips, greetings).
- `hand-lock-sketch.png` — the same poses in thin-line form.

Round black palm blob. Exactly three light-grey rectangular finger tabs plus
one grey thumb tab. Allowed: **open**, **point**, **thumbs-up (palm-closed)**,
**closed at rest**. No grips. If the tool only accepts a few references,
keep these two sheets.

## Examples (page density only)

Low-frequency visual calibration — line density, whitespace, colour restraint,
deadpan-worker energy. Do not copy these compositions (see the no-recycling
rule). Do not use them as the character or hand reference; older files may
still look like Xiaohei.

- `examples/01-vibe-coding.png` — workflow: slips in, crank machine, app on easel, inspector.
- `examples/02-blocks-to-hardware.png` — concept metaphor: block hopper, screw press, walking board.

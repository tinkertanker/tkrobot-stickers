# Hand Scale v2 Notes

Second hand-scale test batch for selected T Krobot poses.

Changes from rejected `hand-scale-v1`:

- Keeps the larger black palm direction.
- Replaces sausage-like fingers with flatter rectangular tabs.
- Reinforces exactly three fingers plus one thumb per visible hand.
- Keeps definitive `stickers/` files unchanged.

Review notes:

- `greetings`, `handraise4`, and `shrug4` are the clearest tests of the rectangular finger direction.
- `gasp` is useful for hand scale, but still has small internal arm crease marks that would need cleanup before promotion.

Files:

- `sources/` contains chroma-key generated sources.
- `stickers/` contains transparent test PNGs.
- `contact-sheet.png` shows only v2 outputs.
- `comparison-sheet.png` compares original, current, rejected v1, and v2 versions.


# V2-Guided Retry Notes

Retry after reverting the over-blocky deterministic renderer pass.

Direction:

- Match the rounded original/v11 T Krobot proportions and the `hand-scale-v2` rectangular hand tests.
- Only the fingers should become flatter, more rectangular, and slightly larger.
- Do not make the head, body, feet, or overall mascot silhouette blocky.
- Each visible hand should have exactly three fingers plus one thumb.
- Keep final promotion separate until visual review passes.

Included:

- `greetings`: copied from accepted `hand-scale-v2` rectangular hand test.
- `handraise`: copied from accepted `hand-scale-v2` rectangular hand test, renamed without the `4` suffix for this retry batch.
- `shrug`: regenerated to keep rounded T Krobot proportions while making both hands show three rectangular fingers plus one thumb.

Not included:

- A fresh `gasp` retry was generated but not saved here because it repeated the unwanted internal arm crease problem. Use the v2 `gasp` only as a hand-scale reference, not as a promoted final.

# V1-Size Blocky Full-Pack Candidates

Full-pack candidate pass using the approved `v1-size-blocky-fingers` direction.

Prompt anchor:

- Keep rejected-v1 hand and finger scale. Do not shrink toward `hand-scale-v2`.
- Change only the fingers into large flat rectangular tabs with hard sides and slightly rounded corners.
- Preserve rounded original/v11 T Krobot proportions and soft cartoon rendering.
- Each visible hand must show exactly three fingers plus one thumb.
- Keep final promotion separate until visual review passes.

Seed anchors copied from `v1-size-blocky-fingers`: `gasp`, `greetings`, `handraise`, `shrug`.

Review notes:

- This batch is the first full-pack pass after crystallising the prompt: rejected-v1 hand/finger size, rectangular-tab finger shape, rounded T Krobot body/head preserved.
- Do not promote wholesale until the contact sheet is reviewed; the visible direction is intentionally captured here for iteration.
- Watch the highly dynamic poses (`crashed`, `ded`, `falling`, `flailing`, `shock`, `yay`) for individual hand readability and digit count at sticker size.
- `gasp` is still the copied candidate from `v1-size-blocky-fingers`; its hand count and scale are useful, but it still has unwanted internal arm crease lines.
- `greetings` and `handraise` were updated after review so they no longer share the same silhouette: `greetings` uses a wider side-wave arm angle, while `handraise` uses a near-vertical raised arm and tip-toe/stretch posture.
- `wrong` was updated after review to make the pose carry the meaning: both arms cross in front of the body, with the red X kept off to the side as emphasis.

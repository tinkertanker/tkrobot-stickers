# V1-Size Blocky Fingers Notes

Retry using `hand-scale-v1` as the hand and finger size reference.

Direction:

- Keep the large v1 palm/finger scale; do not shrink toward v2.
- Change only the fingers from sausage-like tubes into blockier rectangular tabs.
- Preserve rounded original T Krobot head/body proportions and soft cartoon rendering.
- Each visible hand must show exactly three fingers plus one thumb.
- Keep this as a candidate batch until visual review passes.

Review notes:

- `greetings`, `handraise`, and `shrug` are the clearest candidates for the requested direction: v1-sized fingers with flatter rectangular tab shapes.
- `shrug` corrects the prior missing-finger issue: both hands now show four total digits, intended as three fingers plus one thumb.
- `gasp` was regenerated once because the first pass had five light-grey protrusions per hand. The saved candidate has four protrusions per hand, but still has unwanted internal arm crease lines, so treat it as a hand-size/count reference rather than a final.
